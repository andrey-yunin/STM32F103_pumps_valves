# План рефакторинга: Приведение к стандарту DDS-240 (Advanced)

**Цель:** Обеспечить 100% совместимость платы насосов и клапанов с экосистемой DDS-240, включая сервисный слой и отказоустойчивость.

## Этап 1: Архитектурный фундамент (Стандартизация)
- [x] **1.1. Переименование**: Изменение имен файлов и функций `task_command_parser` -> `task_dispatcher`.
- [x] **Синхронизация инфраструктуры**: Переименование очередей в `dispatcher_queueHandle` и `fluidics_queueHandle`.
- [x] **1.2. Модуль конфигурации и идентификации (`app_flash.h/c`)**:
    - [x] Чтение UID чипа (96 бит).
    - [x] Определение Device Type (0x03 - Fluidic) и версии FW.
    - [x] Работа с Page 63 (STM32F103).
    - [x] Структура с Magic Key (0x55AAEEFF) и CRC16.
    - [x] Сохранение/загрузка NodeID и Fluidic Map.
- [x] **1.4. Транспортный уровень (bxCAN)**:
    - [x] Аудит `task_can_handler`: Mailbox Guard (проверка ящиков перед передачей).
    - [x] Поддержка Broadcast (`DstAddr = 0x00`).
    - [x] Переход на динамический NodeID (из `AppConfig`).
    - [x] Реализация `CAN_SendData` с поддержкой EOT.
- [x] **1.5. Архитектурная чистка (Унификация с паттерном)**:
    - [x] Устранение дублирования ACK: Удаление `CAN_SendAck` из `task_pump_controller` (остается только в Диспетчере).
    - [x] Единый источник маппинга: Перевод `device_mapping.c` на работу через `AppConfig`.
    - [x] Консистентность NACK: Замена общих кодов ошибок на специфичные для домена Fluidics.

## Этап 2: Сервисный слой (0xFxxx)
- [x] **2.1. Коды команд**: Синхронизация `can_protocol.h` с эталоном (0xF001-0xF00F, Magic Keys).
- [x] **2.2. Обработка в Диспетчере (`task_dispatcher`)**:
    - [x] `0xF001` (Get Info): Сборка ответа (Type, Ver, UID).
    - [x] `0xF002` (Reboot): NVIC_SystemReset() с проверкой Magic Key `0x55AA`.
    - [x] `0xF003` (Commit): Запись RAM-конфигурации во Flash.
    - [x] `0xF004` (Get UID): Отдельная команда получения серийного номера.
    - [x] `0xF005` (Set NodeID): Динамическое изменение адреса в RAM.
    - [x] `0xF006` (Factory Reset): Сброс настроек и перезагрузка (Key: 0xDEAD).

## Этап 3: Прикладная логика Fluidics (0x3xx)
- [x] **3.1. Расширение структур данных**: Добавление `timeout_ms` в `PumpValveCommand_t` и `ParsedCanCommand_t`.
- [x] **3.2. Доработка Диспетчера после CAN-аудита**:
    - [x] Исправить разбор `timeout_ms`: payload bytes 3-6 должны попадать в `parsed.data[0..3]` и собираться как `uint32_t LE`.
    - [x] Повторно проверить `PUMP_START` с ненулевым timeout: `2000 ms`, автоотключение нагрузки примерно через 2 секунды.
    - [x] Подтвердить команду `0x0201` (`PUMP_RUN_DURATION`): `2000 ms`, нагрузка отработала штатно.
- [x] **3.3. Реализация Safety Timeout (Защита железа)**:
    - [x] Создание пула из 16 FreeRTOS Software Timers.
    - [x] Автоматическое выключение насоса/клапана по истечении времени.
    - [x] Остановка таймеров при получении явной команды `STOP/CLOSE`.
- [x] **3.5. Safe-state при отказе safety timer**:
    - [x] Если таймер канала не создан или `osTimerStart()` вернул ошибку, не включать нагрузку.
    - [x] Вернуть канал в `OFF` и отправить `NACK`, без `DONE`.
    - [x] Зафиксировать для Дирижера: такой отказ является прикладным `NACK`, а не timeout ожидания.
    - [ ] Провести fault-injection тест отказной ветки timer-start.
- [x] **3.4. Маппинг имен**: Уточнение реальных имен насосов и клапанов.

## Этап 4: Верификация и Отчетность
- [x] **4.1. Базовая CAN-валидация через CANable**:
    - [x] SocketCAN поднят на 1 Mbit/s.
    - [x] `GET_DEVICE_INFO (0xF001)`: получены ACK, DATA, DONE.
    - [x] `PUMP_START (0x0202)` и `PUMP_STOP (0x0203)` для канала 0: получены ACK и DONE.
- [ ] **4.2. Тестирование сервисных команд**: Проверка через CAN-анализатор.
    - [x] `0xF004` Get UID: ACK, 2 DATA, DONE; UID совпадает с `GET_DEVICE_INFO`.
    - [x] `0xF002` Reboot с неправильным Magic Key: NACK `0x0004`.
    - [x] `0xF002` Reboot с правильным Magic Key: ACK, DONE, плата снова отвечает на `0x30`.
    - [x] `0xF003` Commit.
    - [x] `0xF005` Set NodeID в RAM: `0x30 -> 0x31 -> 0x30`.
    - [x] `0xF005` Set NodeID + Commit + Reboot persistence: `0x30 -> 0x31 -> 0x30`.
    - [x] `0xF006` Factory Reset с неправильным Magic Key: NACK `0x0004`.
    - [x] `0xF006` Factory Reset с правильным Magic Key: ACK, DONE, default `0x30` восстановлен.
- [x] **4.3. Тестирование Flash**: Проверка сохранения NodeID после Reboot.
- [ ] **4.4. Финальный аудит**: Статус "Fully Compliant" разрешен только после закрытия этапов 6-8.

## Этап 5: Унификация Экосистемы (Директива 2.0)
- [x] **5.1. Ресурсный маппинг (0-based Indexing)**:
    - [x] Обновление `app_flash.c`: замена дефолтных ID (10-25) на индексы (0-15).
- [x] **5.2. Детерминированный транспорт (Strict DLC=8)**:
    - [x] Модификация `task_can_handler.c`: проверка `DLC == 8` для всех входящих команд.
    - [x] Обновление функций ответов (`ACK`, `NACK`, `DONE`, `DATA`): принудительный `DLC = 8`.
    - [x] Зафиксировано протокольное правило: `DLC != 8` и другая невалидная транспортная оболочка игнорируются без ACK/NACK; Дирижер обрабатывает это по ACK timeout.
    - [x] Проверено: короткий DLC, Standard ID и non-COMMAND frame игнорируются без ACK/NACK.
- [x] **5.3. Исправление выравнивания Payload**:
    - [x] `task_dispatcher.c`: исправить сборку `timeout_ms` из `parsed.data[0..3]`.
    - [x] Добавить тестовый кадр с ненулевым timeout и проверить фактическое время автоотключения.
    - [x] `RUN_DURATION` проверен; Дирижер может использовать `0x0201` для timed-run сценариев с ненулевым timeout.
- [x] **5.4. Базовый аудит соответствия**: CAN ID, NodeID, strict DLC=8 и ACK/DONE подтверждены на реальной шине.

## Этап 6: Приемочные CAN-тесты Fluidics
- [x] **6.0. Smoke-test физической нагрузки через CAN-switch**:
    - [x] Топология `CANable -> passive CAN switch -> Fluidics executor` проверена на 1 Mbit/s.
    - [x] Найден и устранен дефект разъема кабеля, ранее вызывавший отсутствие ответов.
    - [x] `load-cycle 0 1 3 1 0.5`: каналы 0 и 1 отработали индивидуально и совместно.
    - [x] После теста шина осталась `ERROR-ACTIVE`, новые `bus-errors` и `bus-off` не появились.
- [ ] **6.1. Полный прогон насосов**:
    - [x] Каналы `0` и `1`: физическая нагрузка подтверждена.
    - [ ] `pump-cycle 0..12`.
    - [ ] Проверка соответствия GPIO фактической разводке.
- [ ] **6.2. Полный прогон клапанов**:
    - [ ] `valve-open 13..15`.
    - [ ] `valve-close 13..15`.
- [ ] **6.3. NACK matrix**:
    - [x] Насосная команда на клапанный индекс `13`: NACK `0x0002`.
    - [x] Клапанная команда на насосный индекс `0`: NACK `0x0002`.
    - [x] Индекс вне диапазона `16`: NACK `0x0002`.
    - [x] Неизвестная команда `0x9999`: NACK `0x0001`.
    - [x] Неверный Magic Key: NACK `0x0004`.
    - [x] Недопустимые `SET_NODE_ID` значения `0x00/0x01/0x10/0x80`: NACK `0x0001`.
    - [ ] Расширить NACK matrix на остальные граничные индексы при полном прогоне каналов.
- [ ] **6.4. Broadcast и адресация**:
    - [x] Прием `DstAddr=0x00`: broadcast `GET_DEVICE_INFO` возвращает ACK, DATA, DONE.
    - [x] Игнорирование чужого `DstAddr=0x20`.
    - [x] Проверка после `SET_NODE_ID`: старый адрес молчит, новый отвечает.

## Этап 7: Отказоустойчивость и промышленная готовность
- [ ] **7.1. Watchdog / Failsafe**:
    - [x] Добавить `PumpsValves_AllOff()` и вызывать его при старте, `Error_Handler()` и fault/error paths.
    - [x] Проверять результат `osTimerStart`; если safety timer не стартовал, вернуть канал в safe state и отправить `NACK` без `DONE`.
    - [x] Провести нормальную регрессию после safe-state hook: `PUMP_START/STOP` и `RUN_DURATION 2000 ms` PASS.
    - [ ] Провести controlled fault-injection входа в fault handlers.
    - [ ] Включить аппаратный IWDG.
    - [ ] Обслуживать IWDG только при штатной работе CAN, Dispatcher и Application tasks.
    - [ ] Зафиксировать безопасное состояние GPIO после reset.
    - [ ] Определить поведение при потере связи после `START`.
- [ ] **7.2. CAN fault handling**:
    - [ ] Добавить общий сервисный код `0xF007 GET_STATUS`.
    - [ ] Добавить диагностические счетчики CAN ошибок: error callback, warning, passive, bus-off, last HAL error, last ESR.
    - [ ] Добавить счетчики переполнения RX/TX/Dispatcher/Fluidics queues.
    - [ ] Добавить счетчики transport drop: not Extended ID, foreign dst, non-COMMAND, wrong DLC.
    - [ ] Отдавать статус через DATA-метрики `metric_id:uint16 LE + value:uint32 LE`.
    - [ ] Провести регрессию `GET_STATUS`, затем штатные `GET_INFO`, `PUMP_START/STOP`, `RUN_DURATION`.
- [x] **7.3. Flash safety**:
    - [x] Зарезервировать последнюю страницу Flash в linker script или отдельной секции.
    - [x] Проверить сборку после ограничения Flash application region до `63K`.
    - [x] Повторить post-flash runtime-регрессию `COMMIT -> REBOOT -> GET_INFO`.
- [ ] **7.4. Физический уровень**:
    - [x] Проверка через пассивный CAN-switch на текущем стенде.
    - [x] Проверка кабеля/разъема после выявленного дефекта.
    - [ ] Проверка терминаторов 120 Ом.
    - [ ] Проверка общей земли или изоляции CAN.
    - [ ] TVS-защита CANH/CANL.
    - [ ] Нагрузочный тест на рабочей длине кабеля.

## Этап 8: Документация экосистемы и регрессия
- [x] **8.1. Единый источник документации экосистемы**: основные файлы перенесены в `readme/DDS-240_eko_system`, внешние дубликаты удалены.
- [x] **8.2. Разделение уровней протокола**: `Host -> Conductor` остается с динамическим DLC, `Conductor -> Executor` использует strict DLC=8.
- [x] **8.3. Послания Дирижеру для промышленного режима**:
    - [x] ACK timeout отделен от NACK.
    - [x] Дирижер обязан иметь operation timeout поверх safety timeout исполнителя.
    - [x] Зафиксирован переход `SET_NODE_ID`: ACK со старого NodeID, DONE с нового NodeID.
    - [x] После `COMMIT/REBOOT/FACTORY_RESET` требуется повторный discovery.
    - [x] `RUN_DURATION (0x0201)` разрешен Дирижеру как production-сценарий с ненулевым timeout; `DONE` означает старт GPIO/таймера, а не момент автоотключения.
- [ ] **8.4. Обновить ссылки на удаленные дубликаты**:
    - [ ] `README.md`.
    - [ ] `readme/project_report.md`.
- [ ] **8.5. Регрессионная сверка протокола**:
    - [ ] `App/inc/can_protocol.h` vs `readme/DDS-240_eko_system/can_protocol_pumps.h`.
    - [ ] Command codes, NodeID, Device Type и NACK-коды.
    - [ ] SocketCAN examples vs фактические ответы прошивки.
